
<div class="card-body d-flex flex-column">
    <div class="event-card-title mb-2">{{ product.name }}</div>
    <p class="text-info fw-bold mb-0">{{ (product.price / 100)|number_format(2, ',', ' ') }} €</p>
</div>