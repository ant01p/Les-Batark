{% extends 'base.html.twig' %}

{% block title %}Boutique — Les BATARK{% endblock %}

{% block body_class %}events-page{% endblock %}

{% block body %}

<section class="bg" style="background-image: url('{{ asset('images/shop.png') }}')">
    <div class="bg-overlay">
        <div class="container">

            {{ include('_shared/_flash.html.twig') }}

            <div class="page-header mb-5">
                <div class="text-center mb-3">
                    <img src="{{ asset('images/LESBATARK.png') }}" alt="Logo LES-BATARK" class="logo-batark">
                </div>

                <div class="page-title">
                    <h1 class="text-white">Boutique</h1>
                </div>
            </div>

            {% if categories is empty %}
                <div class="card border-secondary content-card text-center py-5">
                    Aucun produit disponible pour le moment.
                </div>
            {% else %}

                <div class="d-flex flex-wrap gap-2 mb-4">
                    {% for category in categories %}
                        <a href="{{ path('app_shop', { category: category.id }) }}"
                           class="btn btn-primary{{ activeCategory and category.id == activeCategory.id ? ' active' : '' }}">
                            {{ category.name }}
                        </a>
                    {% endfor %}
                </div>

                <hr class="border-secondary mb-4">

                {% if products|length > 0 %}
                    <div class="row g-3">
                        {% for product in products %}
                            <div class="col-6 col-md-4 col-lg-3">

                                <a href="{{ path('product_show', { id: product.id }) }}" class="text-decoration-none">
                                    <div class="card border-secondary shop-card h-100">

                                        <div class="card-body d-flex flex-column">
                                            <h2 class="mb-2">{{ product.name }}</h2>
                                            <p class="text-info fw-bold mb-0">{{ (product.price / 100)|number_format(2, ',', ' ') }} €</p>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <div class="card border-secondary content-card text-center py-4">
                        Aucun produit dans cette catégorie pour le moment.
                    </div>
                {% endif %}

            {% endif %}

        </div>
    </div>
</section>

{% endblock %}
